# 改善一覧

## v2.0（2026-09-23）

前回（2026-09-06）の「今後の提案」のうち 1（アンカー編集）、3（動画書き出し）、5（同梱による完全オフラインの顔追跡・キャリブレーション）を実装し、OBS連携・操作性・自動リグ・描画品質を全体的に見直した。

### 新機能

- [x] **OBS同期**: 編集画面で開いたPSDを `obs_server.py` 経由でOBS表示へ送り、パラメーター・表情・レイヤー・アンカー・自動動作も随時反映。後から開いたOBSソースも現在の状態に追いつく。マイクの音量も中継し、OBS側で口パクする。`?model=` / `&settings=` による単体表示にも対応。ヘッダーに接続状況（表示中のOBS数）を表示
- [x] **顔追跡の強化**（`lib/face-features.js`）: 左右別のまばたき（ウインク）、眉の上下、笑顔（口の形）、One Euroフィルタによる手ブレ抑制、正面キャリブレーション、頭・目・口・眉・視線の感度、なめらかさ。縦横比を補正した計測。カメラ映像と検出点のプレビュー
- [x] **MediaPipe FaceMesh の同梱と版固定**（0.4.1633559619、SIMD版）。オフラインで動作し、CDNの更新で壊れない
- [x] **アンカー編集モード**: 顔の中心・左右の目・閉じ位置・口・首の回転軸・胸をドラッグ／矢印キーで調整。静止ポーズで表示し、閉じ位置のドラッグ中はその目を閉じてプレビュー。設定・JSON・OBS同期・履歴の対象
- [x] **動画書き出し**（`lib/recorder.js`）: 元のPSDサイズで透過WebM（VP9/VP8）またはMP4。長さ・fps・形式・背景を選択。PNGにも背景選択を追加
- [x] **元に戻す／やり直す**（Ctrl+Z / Ctrl+Shift+Z、ボタン）と未保存マーカー・離脱確認
- [x] **キーボードショートカット**: 表情 1〜7 / 0、Space、Ctrl+S / Ctrl+O、E、F、B、? で一覧
- [x] **プレビューの拡大・移動**（ホイール・ピンチ・ドラッグ、F／ダブルクリックで全体表示）
- [x] **レイヤーパネル**: サムネイル、役割表示（例「前髪 · 房6」「閉じ目 · 左 · 自動生成」）、元のPSDレイヤー名、ドラッグ並べ替え、詳細の折りたたみ、ホバーで範囲表示
- [x] **モデル診断**: 必須レイヤーの有無・自動生成・未分類レイヤーを一覧表示
- [x] **自動リグ**: 日本語・英語の別名（前髪・白目・瞳・まつ毛・閉じ目・bangs・iris など）、see-through の `hair` を描画順で前髪／後髪に振り分け、横髪・アホ毛・眼鏡・首元・脚・靴・尻尾・翼・小物の枠を追加。耳飾り・尻尾の振り子の揺れ（「小物の揺れ」）。髪が体の傾きに遅れて追従
- [x] 設定JSONをプレビューへドロップ／PSDと同じ「開く」から読み込み。設定形式 v2（アンカー追加）。v1 や、レイヤー構成が一部異なる設定も読み込める
- [x] 設定検索を行単位に。セクションの開閉状態を記憶

### 不具合修正

- [x] 白目レイヤーを非表示にする、または白目が無い側の瞳が描画されなかった（ステンシルが立たないため）
- [x] 瞳の切り抜きの縁がギザギザだった → 白目の形をマスクテクスチャに描き、滑らかに切り抜く（`lib/renderer.js`）
- [x] 細いパーツ（耳飾りなど）で房の検出位置が塗りの外に出て、物理が付かなかった（平坦なピークの中心を取り、塗られた列へ補正）
- [x] OBS表示が常に `sample.psd` になり、自分のモデルを配信できなかった
- [x] 顔追跡ライブラリをCDNの最新版で読み込んでいたため、将来の更新で動かなくなる恐れがあった
- [x] ドラッグ中にプレビューの枠表示がちらつく（子要素の dragleave）
- [x] 一時停止中も毎フレーム再描画していた（変更時のみ描画）
- [x] 閉じ目の差分判定を毎フレーム・レイヤーごとに全走査していた
- [x] 中継サーバーが `.git` や `.py` などのファイルも配信できた。`.wasm` のMIMEタイプを明示
- [x] ステータス表示が消えずに残り続けた（自動で薄れる）
- [x] ダウンロード用のリンクをDOMに追加せずクリックしていた（一部ブラウザで保存されない）
- [x] リポジトリに `__pycache__` が含まれていた。`.gitignore` / `.gitattributes`（`.bat` はCRLF）を追加
- [x] 中継サーバー: 不正な要求行でログ処理が例外になる、NaN を含む設定で配信が止まる、`%2E` で隠しファイル判定をすり抜けられる問題を修正
- [x] Ctrl+S / Ctrl+O が入力欄にフォーカスがあるとブラウザ側の保存・開くになっていた。キーの長押しで表情・一時停止が連続で切り替わっていた
- [x] 編集用ブラウザがOBSの後ろに隠れる・最小化されると、ブラウザのタイマー間引きで顔追跡・マイクの中継が止まりやすかった（Workerのタイマーで駆動）

### 構成・品質

- `lib/app.js` から描画・デバイス・録画・OBS同期・顔特徴量を別ファイルに分離
- スタイルシートを1つのテーマに統合（3段の上書きを解消）
- テスト: Node（リグ・ランタイム・顔特徴量）、Python（中継 9件）、ブラウザ内 47項目、Playwright による OBS同期と偽Webカメラでの顔追跡のE2E。GitHub Actions を追加。`npm test` / `npm run test:browser`
- 読み取り専用の診断API `window.Anime25D.state()`（テスト・不具合報告用）

### 今後の提案（今回は実装しない）

1. モーションのタイムライン、キー位置保存、補間曲線、音声との同期
2. PSD・差分画像・設定を含むプロジェクト一式の保存とバージョン移行
3. MediaPipe Tasks（FaceLandmarker）の表情ブレンドシェイプによる、さらに細かな表情追跡
4. 表示解像度に合わせた描画（大きなPSDを小さな画面で表示するときの負荷と縮小ノイズの軽減）

---

## v1.x（2026-09-06）

公開リポジトリとローカル版を確認。OBS連携・長い閉じ目差分を含むローカル版を基準にする。
既存の独自2.5D処理を改良し、外部アバター製品のSDK・形式・コードは導入しない。

### 実装内容

- [x] PSDの進捗表示、中断、連続選択への対応、失敗時のモデル保持
- [x] モデル別の調整をブラウザ内に保存・復元、設定JSONの入出力
- [x] 透過PNG書き出しと一時停止
- [x] レイヤーの表示、不透明度、奥行き、順序を編集・保存
- [x] 設定検索、数値直接入力、初期値復帰
- [x] 黒・赤を基調としたUI整理、常設ファイル操作、狭い画面・キーボード対応
- [x] 空レイヤー、特殊な名前、片側の閉じ目補完、細い髪の房重複の修正
- [x] 半透明画像の合成と拡大縮小の色にじみ修正
- [x] 描画順から独立した左右の瞳クリップ、長い閉じ目の位置・角度補正
- [x] 低FPS時のバネ安定化、GPUバッファ再利用とリソース管理
- [x] カメラ・マイクの停止／準備中の取消、追跡途絶からの復帰
- [x] OBS中継の入力・接続元検証、通常の静的公開時の不要な中継通信を抑制
- [x] ファイル分割、主要な回帰テスト、実PSDによる検証、説明書更新

#### 検証と追加修正（2026-09-07）

- Node: 空レイヤー・特殊名・片目補完・半透明画素、設定検証、細長いメッシュ、10〜144fpsのバネ計算を確認。
- 2つの実PSDをWorkerと同じ処理で解析し、軽量PSDの書き出し／再読み込みと合成プレビューを確認。汎用差分を含めて sample.psd は17パーツ、sample2.psd は20パーツ。
- ブラウザ: 仮想デバイスの停止・準備中取消、設定保存／JSON、透過PNGの画素、描画順変更後の左右の瞳、不正PSD／取消後のモデル保持、GPU復旧、390px幅での横はみ出し無しを確認。
- Python: 中継のプロトコル、入力形式・サイズ・接続元、SSEでの追跡停止通知を確認。
- 追加修正: 描画復旧時に旧GPUハンドルへアクセスしないよう修正。起動バッチはPython本体の存在を検証し、サーバーのポートも変更可能にした。

実際のカメラの追跡精度・OBSアプリ内での配信は、この検証には含めない。ブラウザと中継の接続・入力処理はテストで確認している。

### 当時の今後の提案

1. 顔・目・口・首のアンカーをキャンバス上でドラッグ調整する編集モード。
2. モーションのタイムライン、キー位置保存、補間曲線、音声との同期。
3. フレームレート・背景・透過方式を指定した動画書き出し。
4. PSD・差分画像・設定を含むプロジェクト一式の保存とバージョン移行。
5. 顔追跡モデルを同梱した完全オフラインモードと追跡キャリブレーション。

これらは保存形式や編集体験の設計を伴うため、独立した次期機能として扱う。
